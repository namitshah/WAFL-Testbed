name: Ruff
on:
  push:
    paths-ignore:
      - 'DETR/wafl/src/common/**'
  pull_request:
    paths-ignore:
      - 'DETR/wafl/src/common/**'
jobs:
  ruff:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/ruff-action@v3
